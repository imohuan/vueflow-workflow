完善代码节点
节点高度有问题

太棒了
现在修复小问题

1. 删除节点 UI 中的类型声明
2. 你的代码编辑器 支持 ctrl + 鼠标滚动进行缩放，
3. 编辑器 js 代码提示
4. 请将节点配置中的类型声明 进行配置，我希望看提示效果， 我不清楚你的 monaco 使用的是 js 还是 ts 模式，我希望可以在代码中执行，希望你的编辑器是 typescript 模式，然后通过浏览器编译成 js 进行执行，
5. 在全屏的左侧添加一个重置按钮，恢复最原始的代码 你的全屏 UI 可以参考节点配置 UI(复刻 节点配置的宽高，顶部状态栏样式，内容区域就显示编辑器）

需要注意，不能影响下面提到的功能

新增输入时的“编辑态保护”：Monaco 编辑器与类型声明文本域在获得焦点时只更新本地状态，不再即时写回 store；失焦后才一次性落库，避免重绘造成光标和历史记录的丢失。
挂起刷新机制：若停留在编辑态，写回请求会被标记为待处理并在失焦后执行，确保不会中断正在输入的内容。
调试日志优化：保留关键日志（如 event:monacoFocus/Blur、debounced:updateCode deferred 等），方便继续定位。新增 isCodeSyncPending、isTypeDeclarationSyncPending、isCodeEditorFocused、isTypeDeclarationFocused 状态。
其它整理：去掉导致 ESLint 报错的多余入参，清理未使用依赖，保持配置面板行为稳定。
